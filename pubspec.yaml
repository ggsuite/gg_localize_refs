name: gg_localize_refs
description: gg_localize_refs allows to switch a local package and its local checkouts to local pathes
version: 4.5.0
repository: https://github.com/ggsuite/gg_localize_refs.git

environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  args: ^2.7.0
  gg_args: ^3.2.0
  gg_console_colors: ^2.5.0
  gg_log: ^1.2.0
  gg_process: ^1.3.0
  path: ^1.9.1
  yaml: ^3.1.4
  yaml_edit: ^2.2.4
  gg_project_root: ^2.2.0
  pubspec_parse: ^1.6.0
  gg_git: ^4.4.1
  gg_publish: ^4.7.0
dev_dependencies:
  dna_ggsuite: ^0.2.1
  gg_capture_print: ^1.2.0
  helix: ^1.7.1
  lints: ^6.1.0
  test: ^1.32.0

executables:
  gg_localize_refs: