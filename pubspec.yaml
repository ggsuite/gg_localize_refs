name: gg_localize_refs
description: gg_localize_refs allows to switch a local package and its local checkouts to local pathes
version: 4.2.0
repository: https://github.com/ggsuite/gg_localize_refs.git

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  args: ^2.7.0
  gg_args: ^3.0.0
  gg_console_colors: ^2.3.0
  gg_log: ^1.0.6
  gg_process: ^1.1.6
  path: ^1.9.1
  yaml: ^3.1.3
  yaml_edit: ^2.2.4
  gg_project_root: ^2.0.0
  pubspec_parse: ^1.5.0
  gg_git: ^4.1.0
  gg_publish: ^4.3.0
dev_dependencies:
  gg_capture_print: ^1.0.9
  lints: ^6.1.0
  test: ^1.31.2

executables:
  gg_localize_refs: